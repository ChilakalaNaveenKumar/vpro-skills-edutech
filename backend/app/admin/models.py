"""No models of its own.

Admin endpoints (added in Phase 8) manage the same tables the other
modules own (courses, batches, topics, questions, users) - there is
intentionally no separate "admin" table.
"""
